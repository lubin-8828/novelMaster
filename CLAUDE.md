# CLAUDE.md — novelMaster 项目说明

## 项目是什么

novelMaster 是一个跑在终端里的 AI 辅助小说写作工具，单人单机使用。

它把小说创作变成一条有持久化状态、每步都要用户拍板的流水线：
**想法 → 大纲 → 事件推演 → 章节大纲 → 正文 → 审查 → 去 AI 味 → 用户验收 → 资料回填 → 清空上下文 → 下一章**

技术形态：TypeScript（ESM）+ pi agent SDK。它不是独立应用，而是一个 **pi 扩展**，由 pi 的 `InteractiveMode` 作为 TUI 宿主运行。运行时需要 Node >= 22.19（依赖原生 TypeScript 类型剥离，因此没有构建步骤）。

## 设计文档位置

本项目的权威设计文档为：**`DESIGN.md`（项目根目录）**。

本文档已按规则 4 树状拆分：

- 根文档 `DESIGN.md`：项目概述、名词表、全局命名与约定、文件结构、模块导航索引、一致性约束；
- 模块级细节在 `docs/design/` 下的 7 个子文档中。

**加载方式：先读根文档 `DESIGN.md`，再按任务从它的「模块导航索引」定位需要的子文档，按需加载，不要全量读取。**

| 子文档 | 何时该读 |
|--------|----------|
| `docs/design/architecture.md` | 要动代码结构、加依赖、跑起来调试 |
| `docs/design/data.md` | 要读写任何资料、改数据结构 |
| `docs/design/ai.md` | 要碰 AI 调用、提示词、上下文装配 |
| `docs/design/interaction.md` | 要加命令、改层面行为、改交互流程 |
| `docs/design/pipeline.md` | 要动写作流水线、审查规则、去 AI 味 |
| `docs/design/ops.md` | 测试验收、排查故障、规划下一步 |
| `docs/design/decisions.md` | **动手前必读** —— 防止把已否决的方案重新发明一遍 |

## 强制规则（任何 Agent 会话必须遵守）

### 规则 1：动手前必读设计文档

在任何任务动手之前，必须先完整阅读设计文档（见上方位置说明）。
不允许在未阅读设计文档的情况下修改任何文件。

### 规则 2：先改设计，再改代码

修改项目或新开发内容时，必须先修改设计文档，并保证设计文档逻辑自洽
（命名约定、数据结构、流程、接口约定前后一致、无矛盾），然后再修改代码。
不允许先写代码后补文档。

### 规则 3：代码必须与设计文档一致

代码开发或修改完成后，必须核对代码与设计文档的一致性；
如果不一致，必须以设计文档为标准修改代码。
若发现设计文档本身有误，应先在设计文档中修正设计并说明原因，再同步代码。

### 规则 4：树状拆分设计文档

项目内容逐渐庞大后，必须按照树状结构拆分设计文档，详细程度由粗到细，
以便 Agent 按需加载知识库：

- 根文档（`DESIGN.md`）保持精简：项目概述、全局约定、模块导航索引；
- 模块细节下沉到子文档（`docs/design/<module>.md`，可按需继续细分）；
- 每级文档双向导航：子文档注明父文档位置，父文档索引全部子文档；
- 拆分后更新本文件中的设计文档位置说明，拆分过程同样遵循规则 2（先改文档再改代码）。

## 开发速览

```bash
npm install          # 装依赖（无原生构建步骤）
npm start            # 启动 TUI
npm test             # 全部测试（771 项断言），不启动 TUI
npm run typecheck    # tsc --noEmit，含 src/ 与 tests/，纯类型检查
```

### 必须知道的五条

1. **没有构建步骤。** `node src/cli.ts` 直接跑，相对导入写显式 `.ts` 后缀。`tsc` 只做类型检查、不产出文件。
2. **不能用 `enum` / `namespace` / 构造器参数属性。** Node 的类型剥离不做代码生成，写了会在运行时直接报错。
3. **没有热更新。** 扩展是通过 `extensionFactories` 内联注册的，pi 的 `/reload` 不会重载它 —— 改完源码必须重启进程。
4. **改动必须跑验证。** `npm run typecheck && npm test`，不要只靠手工点 TUI —— 手工点不可复现。
5. **改数据结构先改 `src/data/schema.ts` 里的 typebox 定义。** 那里是数据结构的**唯一来源**，TS 类型由 `Static<>` 推导 —— 不要另外手写 interface。**新增对象必须写 `additionalProperties: false`**（typebox 默认允许额外属性，不写就等于放行字段名变体）。

### 三条容易被踩的硬约束

- **新命令名不得与 pi 内建命令冲突**（`/new` `/compact` `/quit` `/settings` `/model` `/reload` …）。撞名会被 pi 后缀化成 `/quit:1`，用户体验直接崩。`tests/smoke.ts` 里有机械校验，加了新命令跑测试即可发现。
- **命令名与层面归属只改 `src/extension/layers.ts`**。该文件里 `COMMANDS`（命令自身）与 `LAYER_COMMAND_ORDER`（归属与顺序）是两张表，注册循环遍历 `COMMANDS` 的键 —— 不要改成按层面遍历注册，否则跨层命令会被重复注册。
- **不要绕过工具直接写小说数据**。LLM 侧由护栏拦住了 `write`/`edit` 写小说根（shell 不在拦截范围内，见 `data.md「写入护栏」`）；人改代码时同样要遵守：所有落盘走 `src/data/*` 的写入函数，它们负责 schema 校验、ID 分配、追加式保行与操作留痕。直接 `writeFileSync` 拼一份 `index.json` 会绕过全部保证。

### 当前进度

**11 个里程碑全部交付。** 主链路完整可用：

```
/init → /outline → /event → /write → /next → 生成正文 → 审查 → 去 AI 味
  → 用户验收 → 回填 → /done → 清空上下文 → 下一章
```

23 个 `novel_*` 工具注册给主会话（另有 3 个子会话专用）、写入护栏生效、上下文装配器可检查、771 项断言全过。
下一步是真实写一章的端到端验收（`docs/design/ops.md「手工」`的 61–91 项）。
详见 `docs/design/ops.md「里程碑与实施进度」`。
