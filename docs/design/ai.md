# ai — AI 层与上下文装配器

> 父文档：[`DESIGN.md`](../../DESIGN.md)
> 审查规则与写作流水线见 [`pipeline.md`](pipeline.md)；数据来源见 [`data.md`](data.md)。

---

## 1. 会话类型与生命周期

| 类型 | 创建方式 | 生命周期 | 用途 |
|------|----------|----------|------|
| **主会话** | `InteractiveMode` 持有，`SessionManager.create(cwd)` | 跨命令存活；每章一次 `newSession()` | 用户在层面里的对话、大纲讨论、正文生成 |
| **一次性子会话** | `createAgentSession({ sessionManager: SessionManager.inMemory(), tools, customTools, resourceLoader })` | 单次任务，用完 `dispose()` | 审查员、脑暴角色、去 AI 味 |

子会话的三条特征：

- `SessionManager.inMemory()` —— 不落盘、不带历史，每次都是干净的。
- `tools` 白名单 —— 审查员只给 `read` 和 `novel_read_index`，**不给任何写工具**。这是「审查者不改稿」的代码级实现（见 `pipeline.md「审查引擎」）。
- 不同角色通过 `systemPromptOverride` 注入不同人格。

**子会话与主会话之间有隔离要求**：子会话不得读写主会话的消息历史，只通过「上下文包」和 `submit_*` 工具交换数据。否则「3 个独立视角」会退化成「3 个被前一个污染的视角」。

---

## 2. 每章清空上下文

```typescript
// 章末 /done 成功后
await ctx.newSession({
  parentSession: ctx.sessionManager.getSessionFile(),
  setup: async (sm) => {
    // 这里不注入任何前一章内容 —— 清空就是清空
  },
});
```

新会话是全新的 `AgentSession`，消息历史为空。下一章的上下文完全由「上下文装配器」重建。主会话文件通过 `parentSession` 形成链，便于回溯「当时 AI 看到了什么」。

**约束**：`ctx.newSession()` **只在命令处理器里可用**（`ExtensionCommandContext`），不能在事件处理器里调用（会死锁）。所以清空动作必须由 `/done` 命令驱动，不能挂在 `agent_end` 之类的钩子上。

---

## 3. 模型选择

```typescript
type Purpose = "draft" | "review" | "brainstorm" | "utility";
```

优先级：

1. `state.json` 的 `config.models[purpose]`（用户在层面里可改）
2. `~/.pi/agent/settings.json` 的 `defaultModel` / `defaultProvider`
3. 第一个可用模型

`thinkingLevel`：`draft` 用 `medium`，`review` 用 `high`（判定类任务值得多想），`brainstorm` 用 `medium`。

默认（当前 pi 配置）：全部用 `deepseek` / `deepseek-flash`。用户可以只把 `draft` 换成强模型。

**已知风险**：`deepseek-flash` 写长篇小说正文的质量上限明显吃亏；本项目自用、不设成本上限，所以提供按用途覆盖模型的开关，而不是替用户决定用哪个。是否切换由用户判断。

---

## 4. 提示词分层

系统提示词按四段拼装：

| 段 | 内容 | 何时注入 |
|----|------|----------|
| **基础段** | 项目规则、数据布局说明、可用工具、「不许编造依据」 | 常驻（`DefaultResourceLoader.systemPromptOverride`） |
| **项目段** | 当前小说的书名、根目录、数据布局、硬规则 | `before_agent_start`，仅在已打开小说时 |
| **层面段** | 当前层面的操作规则（例：大纲层只谈结构与冲突，不写正文） | `before_agent_start`，按当前层面 |
| **任务段** | 本回合任务（建书引导 / 推演本章大纲 / 生成正文 / 回填） | 每次命令注入 |
| **子 agent 段** | 角色人格（审查员、脑暴角色） | 子会话创建时 |

项目段与层面段用 `event.systemPromptOptions.sections` 注入，**不整体替换 `systemPrompt`**：pi 会把变化的 section 作为补丁追加，保留其余部分的前缀缓存。整体替换会让每次切层都丢缓存。

已实现的 section key：`novelmaster-project`、`novelmaster-layer`。渲染实现在 `src/extension/render.ts`。

### 4.1 层面数据的装载

呼应「每层只装载该层数据」：层面段注入的是**该层的索引摘要**，不是全量。

| 层面 | 注入 | 状态 |
|------|------|------|
| `/setting` | 设定条目清单（`id` + `name` + `category` + `summary` + `establishedIn`） | ✅ 里程碑 3 |
| `/person` | 人物清单（`id` + `name` + `role` + `status` + `lastUpdatedChapter`）+ 关系清单概要 | ✅ 里程碑 3 |
| `/outline` | **大纲全文** + 事件清单（`id` + `title` + `stage` + `status`） | ✅ 里程碑 4 |
| `/event` | **阶段划分 + 主要冲突**（从大纲提取）+ 事件清单（含伏笔字段） | ✅ 里程碑 5 |
| `/write` | **完整的上下文包**（九段，即 `assemble()` 的产出） | ✅ 里程碑 7 |

**为什么敢全量注入条目清单。** 每条 `id + name + category + summary` 约 30 token，100 条才 3k —— 而截断的代价是 AI 不知道某条目存在，于是编一条重复的（正是审查项 A2 要抓的）。省 token 的方案是**压缩字段**（`summary` 已是一句话），不是**减少条目**。真到了几百条的规模，该做的是给清单分页或加筛选，而不是静默截断。

**注入的是索引，不是详述。** 条目详述（`S-001.md` 等）只在两种时候进入上下文：用户当前正在谈论的那条，以及里程碑 6 装配器判定的「相关条目」。理由：详述动辄几百字，全量注入会淹没重点 —— 而清单只用来回答「有哪些、各自是什么」。

**两个例外，它们注入的是全文：**

| 层 | 注入全文的东西 | 为什么它可以是全文 |
|----|----------------|--------------------|
| `/outline` | `outline.md` | 大纲就是本层的**工作对象** —— 用户说「第二幕节奏不对」时，AI 必须看得到第二幕。给它一份「标题清单」等于把刚摆好的牌又扣上 |
| `/event` | 大纲的「阶段划分」与「主要冲突」两个区段 | 事件是往阶段里放的，看不到阶段就无从判断「这个事件属于哪一幕」 |

判据不是「长不长」，而是**不看到它就无法完成本层的任务**。同样一组数据，在 `/setting` 层是背景知识（给摘要就够），在 `/outline` 层是工作对象（必须给全文）。

**`/outline` 与 `/event` 看大纲的方式不一样。** 内容有重叠，但重点相反：

| | 先给什么 | 为什么 |
|---|---------|--------|
| `/outline` | 大纲全文，后接事件清单 | 本层审的是**大纲**，事件是用来校验大纲站不站得住的 |
| `/event` | 阶段划分 + 主要冲突，后接事件清单 | 本层干的是**事件**，大纲只用来定位「这个事件放哪一幕」 |

所以 `/event` 不注入整份大纲（“结局”之类的区段对「这个事件放哪」没有帮助），而是只抽它需要的两个区段。**区段抽不到时退化为注入整份大纲** —— 大纲的区段结构是用户可改的，抽不到不代表内容不存在，退一步比什么都不给好。

**这部分不走上下文装配器。** 装配器（本文 §5）是为「生成一章正文」服务的重型设施（token 估算、相关性并集、缓存分段）。层面装载只是「让 AI 知道有哪些条目」，一个读索引 + 渲染的函数就够；把它塞进装配器会让装配器多一个与写作无关的调用者，也会为它引入每次切层的开销。

**唯一的例外是 `/write`。** 它的装载内容就是装配器的产出 —— 理由见下。

### 4.2 为什么 `/write` 每回合都要注入完整上下文

其它层注入的是索引摘要，`/write` 注入的是九段全文。这不是预算失控，是**因为这一层每回合都可能是在写正文**：

- 用户说「写吧」时，AI 手里必须有完整上下文 —— 而 `before_agent_start` 无法预先知道哪一句话是「写吧」（也不应该去猜）；
- 完整上下文**只有生成正文时才需要**，而 `/write` 层就是干这个的。拿别的层去等它，等于把「这章该怎么写」拆成两次对话。

代价必须被承认：在 `/write` 层里每说一句话，都会带上这份上下文（几万 token 量级）。
**所以这一层是「干完就走」的地方** —— 写完一章验收后 `/done`，而不是留在里面闲聊。这一条会写在层的规则里，也会在状态栏里体现当前章。

缓存能抵消一部分：第 1–6 段在相邻章节间几乎不变，而 pi 只把变化的 section 作为补丁追加（见 §5.3）。

**`/context` 与 `/write` 装载用的是同一个 `assemble()`** —— 一个是「打印出来给人看」（带总表与截断），一个是「拼接起来给 AI」（纯内容）。两份数据同源，不会出现「你看到的与 AI 看到的不一样」。

实现在 `src/extension/layer-data.ts`，由 `prompt-sections.ts` 注入为 section `novelmaster-layer-data`。

**用户也要看得到同一份清单。** 进入层面时，命令处理器用 `sendMessage` 把清单发进对话（与 `/help` 同一种呈现方式）。理由：用户进 `/setting` 后对着空屏幕不知道该说什么，「有哪些条目、叫什么」是他开口的前提 —— 而这跟「每层只装载该层数据」并不冲突：给用户的清单和给 AI 的清单本来就该是同一份，所以两者共用同一个渲染函数。

---

## 5. 上下文装配器

`assemble({ novelRoot, chapter, eventId, config })` → `ContextBundle`。这是整个系统的中枢：**装配器的质量 = 成书质量**。

### 5.1 装配规格

顺序原则：**从最稳定排到最易变，最「当下」的放最后。**

| # | 段 | 取数规则 | 理由 |
|---|----|----------|------|
| 1 | meta | `title` `genre` `premise` `logline` `pov` `tense`，全量 | 「这本书是什么」的最小锚点，成本极低 |
| 2 | 大纲 | `outline.md` 全文 | 判断是否脱轨的基准；省了它 AI 只看得到局部 |
| 3 | 设定 | **双层**：全部条目的 `summary` + 相关条目全文 | 设定越写越多，全量全文会淹没重点；只给相关的又会让 AI 编新设定（审查项 A2） |
| 4 | 当前事件 | 主事件全文（当前描述 + 细化历史） | 本章要推进的对象 |
| 5 | 相关人物 | 静态档案 + **全量**关键事件时间轴 | 时间轴条目短；截断会让 AI 以为早期经历没发生过 |
| 6 | 相关关系 | 仅**两端都在相关人物集合内**的关系 | 只提一端的关系是无效信息 |
| 7 | 最近 N 章正文 | `chapters/(c-N..c-1).txt` 顺序全文，**N 默认 5、可配置** | 唯一注入原文正文的部分，连续性最主要的来源（也是成本主项） |
| 8 | 更早章节摘要 | `chapters/1..c-N-1.summary.md` 顺序 | 长篇不失忆的代价 |
| 9 | 本章大纲 | `NNN.outline.md`（用户确认版） | 放最后：当下任务最不该被忽略 |

### 5.2 相关性判定（第 3、5、6 段用）

```
相关人物 = 本章大纲标注的 characterIds
         ∪ 主事件涉及的人物
         ∪ 最近 N 章出场人物

相关设定 = 本章大纲标注的 settingIds
         ∪ 主事件涉及的设定
         ∪ 最近 N 章中出现过的设定
```

**全部取并集，不取交集。** 理由：宁可多喂一条人物档案，也不要因为 AI 不知道某个人而写出矛盾。两种错误的代价不对称 —— 多喂一条只是多点 token，漏一条会导致一致性事故，而一致性事故要靠用户逐条审出来。

三个来源的具体取数落点：

| 来源 | 从哪里取 |
|------|----------|
| 本章大纲标注 | `chapters/NNN.outline.md` 开头的结构化头块（`characters` / `settings`） |
| 主事件涉及 | `chapters/index.json` 的 `primaryEventId` → `events/index.json` 的 `characters` / `settings`（回退：`state.json` 的 `currentEventId`） |
| 最近 N 章出场 | `chapters/NNN.summary.md` 的「## 出场」小节（人物行与地点行） |

**主事件为什么要有回退**：写正文前，章节记录的 `primaryEventId` 可能还没设（要靠 `novel_event_link_chapter`）。而 `state.json` 的 `currentEventId` 是更早就能有的信息。两个都没有时，第 4 段为空并**在 `/context` 里显式标出**（而不是静默跳过）。

**「最近 N 章出场」只能从摘要读，不能从正文读。** 摘要的「出场」小节是结构化生成的（小节标题写死在代码里，见 `data.md「chapters/NNN.summary.md」`），而正文里找人物名等于做实体识别 —— 会把「张局」与「张局长」算成两个人。

「本章大纲标注的 characterIds / settingIds」从 `NNN.outline.md` 的结构化头块读取，格式见 `data.md「章节大纲的结构化头块」`。

### 5.3 缓存友好

第 1–6 段在相邻章节间几乎不变，只有 7–9 段会变。稳定部分放前面 = 可命中的 prompt 缓存前缀。

在「每章都要重建上下文」的架构下这不是小优化：**命中的恰好是占比最大的那部分。**

### 5.4 输出与可检查性

```typescript
interface ContextBundle {
  segments: Array<{
    key: string;            // "meta" | "outline" | "settings" ...
    title: string;
    content: string;
    estimatedTokens: number;
    sources: string[];      // ["setting/S-001.md", ...] 供追溯
  }>;
  totalEstimatedTokens: number;
}
```

`/context` 渲染成：

```
本章上下文包（第 12 章）—— 9 段，共约 32,400 tokens（46,800 字）

  1. meta         约   120 tokens（  420 字）  meta.json
  2. 主线大纲     约   860 tokens（1,240 字）  outline.md
  …
  7. 最近 5 章正文 约 28,000 tokens（41,000 字）  chapters/007..011.txt
```

后面按段展开内容，每段**只显示前 12 行并标出总行数**（`…（共 42 行，只显示前 12 行）`）。

`/context <段名>` 只看某一段的全文（如 `/context 大纲`、`/context 7`）。段名或序号都可，匹配不上时列出可用的段名 —— 而不是只说「没找到」。

成本主项是第 7 段（N 章正文），用户要能一眼看到「这章上下文多大」，才能决定要不要把 N 调小。**看不见的东西调不动** —— 装配器不可检查，出了问题就只能靠猜。

**不做全屏展开视图。** 每段的内容本来就来自磁盘上的文件，真要逐字看就直接 `read` 那个文件；为「看一眼」建一套可交互界面，是把工程预算花在一个已经有现成手段的地方（`read` 工具、编辑器、`cat`）。参数式的单段展开足够覆盖「想看完整」这个需求。

### 5.4.1 token 估算

**公式**：`tokens = CJK 字符数 × 1 + 非 CJK 字符数 ÷ 4`，向上取整。

**为什么不用 pi 导出的 `estimateTokens`**：它的公式是 `字符数 ÷ 4`，那是英文导向的（英文 4 字符 ≈ 1 token）。对中文它会算成 `1 汉字 ≈ 0.25 token` —— **低估 4 倍**，而低估会让用户以为自己很宽裕。

**为什么取 1 而不是 0.6**：真实值取决于具体 tokenizer（常用汉字在多数实现里落在 0.6–1.2 之间），而本项目不做 tokenizer 依赖（那是一个为「猜得更准一点」而引入的包）。**估算是为了让你能横向比较并调 N，不是为了算账。** 宁可高估：高估只会让你少塞点正文，低估会让你在不知不觉中撞上上下文上限。

字符数是**精确**的，会一起打出来 —— 想自己换算就换算。

**N 的默认值是 5**（见 `data.md「state.json」`的 `config.recentChapters`）。5 章正文约 1.5–2 万字、约 3–4 万 token，是这个量级下「记得住上下文」与「成本可控」的平衡点。嫌大就调小，嫌 AI 忘了前情就调大 —— 但代价要你自己看得到（这就是 `/context` 存在的理由）。

### 5.5 审查用的裁剪版

**审查员的输入 = 装配产出（九段）+ 本章正文。** 与装配器的唯一差别就在最后那一段：`assemble()` 的第 9 段是「本章大纲」，而审查对象是**正文**，所以审查输入要多一节。

**没有减免。** 审查需要全部九段，尤其是最近 N 章正文 —— 判定时间线矛盾（D1）、伏笔（E2）、信息越界（B3）都要拿前文对比。少了它，审查就只能靠本章自己判断，而那正是「凭感觉」的入口。

所以「裁剪」这个词指的是**审查员不拿任务段与写作用的那套流水线指令**，而不是少给它资料。

---

## 6. 结构化产出：用工具而不是 JSON 文本

子会话的产出通过 `submit_*` 工具提交，参数用 typebox 定义。

```typescript
const submitFindings = defineTool({
  name: "submit_findings",
  description: "提交审查发现。每条必须带依据引用。",
  parameters: Type.Object({
    findings: Type.Array(Type.Object({
      category: Type.String(),          // "A1".."G3"
      severity: Type.Union([
        Type.Literal("blocking"),
        Type.Literal("warning"),
        Type.Literal("note"),
      ]),
      paragraph: Type.Number(),
      phenomenon: Type.String(),
      evidence: Type.Array(Type.String()),  // ["setting#S-001", "chapter#012:para7"]
      suggestion: Type.String(),
    })),
  }),
  execute: async (_id, params) => {
    collector.push(...params.findings);
    return { content: [{ type: "text", text: `已记录 ${params.findings.length} 条` }], details: {} };
  },
});
```

**为什么不用「让模型输出 JSON 块再解析」**：模型可以写出一段「看起来像 JSON 的散文」，解析失败就只能重试或丢弃。工具参数走 schema 校验，模型没有这个机会。

这直接决定了审查报告能不能被**机械汇总和校验** —— 而机械汇总和依据校验正是「不能凭感觉」的实现手段。

---

## 7. humanizer 的注入方式

**决策**：读 `~/.pi/agent/skills/humanizer/SKILL.md` 的**文件内容**，作为去 AI 味子会话的 system prompt 主体。

**不用**「让模型自己决定加载 skill」。**根因**：本项目要求去 AI 味**必然发生**，而 skill 自主加载是模型驱动的 —— 模型这章想不起来加载，去 AI 味就静默跳过了。用户无法从结果上分辨「这章做过去 AI 味」和「这章模型忘了」。

显式注入让「做没做」变成代码事实，而不是模型的心情。

**测试不依赖真实 skill 文件**：`readHumanizerSkill(path)` 接受可注入路径，`DeaiOptions.skillText` 接受注入文本 —— 测试用临时假 skill 文本（含 frontmatter、模式编号、「样本优先于通用规则」接口句）验证代码逻辑，任何机器上都能跑，不需要用户全局目录里恰好装了这个 skill（见 `decisions.md「踩坑记录」`）。真实 skill 的存在性由运行时校验（读不到即报错，不静默跳过）。

skill 内容里有一条关键接口：**用户给文风样本时，样本优先于通用模式规则**（包括它的破折号禁令）。这正是需要的钩子 —— 样本 = 本书已写章节，见 `pipeline.md「去 AI 味引擎」`。

---

## 8. 多 agent 讨论（脑暴）

`interaction.md「多 agent 脑暴协议」`给了协议（谁参与、父层综合不投票）；本节给实现规格。

### 8.1 触发与角色设计

触发是主会话的一个工具 `novel_brainstorm`，参数：

| 参数 | 说明 |
|------|------|
| `angle` | 用户给的讨论方向。**必填** —— 硬约束「没有方向不启动」的落点 |
| `perspectives` | 2–6 个视角，每个含 `name`（视角名）与 `focus`（它关心什么） |

**视角由主会话设计，不由代码写死、也不另起一次 LLM 调用去生成。** 主会话刚与用户聊完，它已经知道「这几条线」指的是什么；让它把视角一并给出，比再起一个会话去猜要准，也省一次调用。

**视角数下限是 2。** 一个视角的「多 agent 讨论」是自欺 —— 用 schema 的 `minItems: 2` 拦掉，而不是靠提示词劝。

### 8.2 角色的输入包

每个角色收到**同一份基线**，加上自己的视角：

| 段 | 内容 |
|----|------|
| meta | `title` / `genre` / `premise` / `logline` / `pov` / `tense` |
| 大纲 | `outline.md` 正文（不含修订记录） |
| 事件清单 | `id` + `stage` + `title` + `status` + `origin` |
| 讨论方向 | 用户给的 `angle` |
| 角色视角 | `name` + `focus` |

**不含设定条目、人物档案、章节正文。** 这不是「省 token」，是**判据不同**：大纲层的讨论是**结构**层面的（「这条线怎么走」），不是在写具体场景；把人物档案全塞进去会稀释重点。

**但角色可以自己取。** 它拿到 `read` 与 `novel_read_index` 工具 —— 需要查某条设定时自己查，而不是让所有角色都背着一份可能用不上的资料。

这与 §5.2 的「取并集不取交集」不矛盾：那两个判据服务的目标不同。审查漏一条会导致一致性事故（而审查员只能看给定的东西）；讨论是探索性的，多一条少一条不致命，且角色**有自取的能力**。

### 8.3 产出

角色通过 `submit_brainstorm` 提交（**只在这个子会话里可见**）：

```typescript
{
  angle: string;                  // 本角色实际采用的视角
  points: Array<{
    claim: string;                // 主张
    reason: string;               // 理由
    risk?: string;                // 这个主张的代价 / 风险
  }>;
}
```

### 8.4 失败处置（与审查同一原则）

| 情形 | 行为 |
|------|------|
| 子会话创建失败 | **中止整个讨论并报错**，不降级为「少一个角色」 |
| 角色没调 `submit_brainstorm` | 记为「**未产出**」并在结果里明示，**不假装它说了话** |
| 角色超时（120 秒） | 中止该角色，结果里同样记为未产出 |

**为何「少一个角色」比「报错」更糟**：用户看到 4 份产出，会以为这是 4 个独立视角的完整结论。而实际只有 3 个 —— 他基于「4 个视角都同意」做出的判断是假的。这与 `ops.md「失败时绝不静默降级」`是同一条。

**「未产出」与「创建失败」为何区分**：前者是模型没按格式交付（可容忍，如实标注即可）；后者是设施起不来（说明环境有问题，继续跑没有意义，就该停）。

**但「未产出」有两种完全不同的成因**，不要把它们混成一种：

| 成因 | 处置 |
|------|------|
| 模型没按格式交付（跑完了、只是没调工具） | 如实标注为未产出 |
| 模型想交但交不上（工具没注册、调用被拒） | **这是设施问题**，不该伪装成模型行为 |

**判据不是「有没有产出」，而是「模型说了什么」**。所以未产出的 reason 里要附上子会话的最后一段输出 —— 让「它只是没交」与「它想交而交不上」在结果里可区分。这比在代码里嗅 “not found” 之类的字符串稳得多（后者会被模型的措辞变化打穿）。

### 8.5 并发与进度

角色**并发**跑（`Promise.all`）：串行 4 个角色会让用户干等几分钟，而它们之间没有依赖。

等待期间用 `onUpdate` 报告进度（「2/4 完成」）。**没有进度反馈的长等待等于卡死** —— 用户分不出它在跑还是在等。

超时用 `Promise.race` 实现（`session.prompt()` 自身不带超时），超时后 `session.abort()` 再 `dispose()`。

### 8.6 父层综合 = 主会话，不引入 LLM 汇总层

工具把各角色的产出（按角色分组）返回给主会话，由**主会话**做综合：共识 / 真实分歧 / 被忽略的选项 / 新暴露的假设。

**不另起一个 LLM 做汇总。** 理由与 `decisions.md` 的「不做 LLM 汇总层」一致：多一层 LLM 只会引入新的编造风险，而主会话本来就在跟用户对话、本来就有全部上下文。

### 8.7 可测试性：把「起会话」做成可注入的依赖

编排逻辑的关键依赖是 `runRole`：`(role, input) => Promise<RoleOutput>`。

- 生产环境注入真实实现（起子会话）；
- 测试注入假实现（返回固定产出 / 抛错 / 干脆不提交）。

这样「并发、失败处置、未产出标记、结果组装」这些**逻辑**都能自动测，不需要真调模型。剩下不可测的只有「讨论得好不好」—— 那本来就只能由用户判断。
